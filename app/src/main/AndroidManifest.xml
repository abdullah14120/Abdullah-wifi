<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.abdullah.wifibridge">

    <!-- ============================================================= -->
    <!-- 1. أذونات الشبكة والإنترنت الأساسية                         -->
    <!-- ============================================================= -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- ============================================================= -->
    <!-- 2. أذونات الموقع الجغرافي (مطلوبة لـ LocalOnlyHotspot/Wi-Fi) -->
    <!-- ============================================================= -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ============================================================= -->
    <!-- 3. أذونات الأجهزة المجاورة (Android 12+ / API 31+)            -->
    <!-- مخصص بمعامل neverForLocation لتفادي مشاكل واجهة Redmi/MIUI   -->
    <!-- ============================================================= -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />

    <!-- ============================================================= -->
    <!-- 4. أذونات خدمات الخلفية والإشعارات (Android 13+ & 14+)        -->
    <!-- ============================================================= -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- تحديد نوع الخدمة الأمامية وفقاً لمتطلبات Android 14 (API 34) -->
    <uses-permission 
        android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" 
        tools:targetApi="upside_down_cake" />

    <!-- إذن إرسال الإشعارات (Android 13+ / API 33) -->
    <uses-permission 
        android:name="android.permission.POST_NOTIFICATIONS" 
        tools:targetApi="tiramisu" />

    <!-- إذن تجاوز تحسينات البطارية لضمان استمرار عمل سيرفر البروكسي -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- ============================================================= -->
    <!-- هيكل التطبيق والمكونات (Application Elements)                -->
    <!-- ============================================================= -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WiFiBridge"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- الشاشة الرئيسية للتطبيق (MainActivity) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ========================================================= -->
        <!-- خدمة خادم البث والجسريات (Hotspot Foreground Service)      -->
        <!-- ========================================================= -->
        <service
            android:name=".server.HotspotService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"
            tools:targetApi="upside_down_cake" />

    </application>

</manifest>
